version: "3.8"
services:
  grafana:
    image: grafana/grafana:9.5.0
    container_name: grafana_so1
    ports:
      - "3000:3000"
    volumes:
      - ../go-daemon/data:/var/lib/grafana/data
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
    restart: unless-stopped
